# Dependencies
node_modules/
.pnp
.pnp.*

# Build output / caches (Next static export, Vite, TS)
.next/
out/
build/
.vite/
*.tsbuildinfo
next-env.d.ts

# Content mirror synced into the engine by the CLI. The repo root (rooms/,
# worlds/, music/) is the source of truth; engine/public/{rooms,worlds,music}
# is a build artifact.
engine/public/rooms/
engine/public/worlds/
engine/public/music/

# Secrets — keep the key out of git (see .env.example)
.env
.env.*
!.env.example

# Editor / OS cruft
.DS_Store
*.pem
.vercel
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python
__pycache__/
*.pyc
.venv/
