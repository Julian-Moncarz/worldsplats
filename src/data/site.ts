// Deploy-level config. The renderer's primitive is a single room; "which room do
// you land on at the site root" is a per-deploy choice, not part of the room model.
// (A museum is just rooms linked by exits — there is no global start_room.)
export const LANDING_ROOM = 'study';
